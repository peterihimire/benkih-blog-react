import React from "react";
import PostContainer from "../components/PostContainer";

const Posts = () => {
  return (
    <section>
      <PostContainer />
    </section>
  );
};

export default Posts;
